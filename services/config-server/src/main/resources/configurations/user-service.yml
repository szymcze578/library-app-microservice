spring:
  datasource:
    url: jdbc:postgresql://localhost:8051/users
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database: postgresql
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-description: "init_user"
    baseline-version: 0
    locations: classpath:db/migration
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}
server:
  port: 8082